// @ts-ignore
import chaiHttp = require('chai-http');
import * as sinon from 'sinon';
import * as chai from 'chai';
import Match from '../database/models/Match';
import { Response } from 'superagent';

const { expect } = chai;

import { app } from '../app';

chai.use(chaiHttp);

describe('Teams Success cases all Leaderboard', () => {
  it('should be', async () => {
    
  });
});